#Make it use the GitHub SDK



#Create a plan agent which then call the architect(draw.io mcp server) agent, developer, test and deployment (Azure MCP server) agents. 
#The plan agent should create a plan and stop until approved by a human. 
#This should be a config setting in the flow inbetween if you don't want any human intervention

#This needs to handle a flow blow full stack application

What if you submit requirements and the LLM has questions? 
You need to display those questions so the user can answer them. OR 
allow the workflow to go back to the beginning so the requirements can be resubmitted